# Copy this file to `.env` and fill with your values.
# IMPORTANT: never commit your real `.env` containing secrets to source control.

# Example MongoDB connection string (Atlas):
# mongodb+srv://<DB_USER>:<DB_PASS>@<cluster-host>/<database>?retryWrites=true&w=majority
MONGO_URI="mongodb+srv://ashiqurrahmantusher007_db_user:1cfVIvBWmj4ppRGP@test.aajg8xr.mongodb.net/coffee-shop?retryWrites=true&w=majority"

# Admin demo credentials (change for production):
ADMIN_USER=admin
ADMIN_PASS=admin123

# Server port
PORT=5000
